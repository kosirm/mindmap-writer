- detailed data structure
- hierarchy: root node is document name
- for mindmap - side ✅
- for mindmap - orientations
- for mindmap - left right side (every root node - 1'st level)
- for all nodes - sibling order
- for some views - width/height, position
- printable data structure (from store)

OPTIONAL FIELDS USED BY VIEWS:
- for nodes (optional) datetime
- for nodes (optional) location
- for nodes (optional) person
- for nodes (optional) image
- for nodes (optional) video
- for nodes (optional) audio
- for nodes (optional) file
- for nodes (optional) link - many link types available (inter-map, external, etc.)
- for nodes (optional) priority
- for nodes (optional) tags
- for nodes (optional) comments
- for nodes (optional) rating
- for nodes (optional) status
- for nodes (optional) reminder
- for nodes (optional) task
- for nodes (optional) habit
- for nodes (optional) goal
- for nodes (optional) project
- 



- remove ThreePanelContainer (no longer needed)
